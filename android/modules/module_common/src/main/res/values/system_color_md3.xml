<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Lightest shade of the accent color used by the android_system. White.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_0">#ffffff</color>
    <!-- Shade of the accent android_system color at 99% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_10">#F1FFFC</color>
    <!-- Shade of the accent android_system color at 95% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_50">#9CFFF2</color>
    <!-- Shade of the accent android_system color at 90% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_100">#8DF5E3</color>
    <!-- Shade of the accent android_system color at 80% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_200">#71D8C7</color>
    <!-- Shade of the accent android_system color at 70% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_300">#53BCAC</color>
    <!-- Shade of the accent android_system color at 60% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_400">#34A192</color>
    <!-- Shade of the accent android_system color at 49% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_500">#008375</color>
    <!-- Shade of the accent android_system color at 40% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_600">#006C5F</color>
    <!-- Shade of the accent android_system color at 30% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_700">#005747</color>
    <!-- Shade of the accent android_system color at 20% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_800">#003E31</color>
    <!-- Shade of the accent android_system color at 10% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_900">#002214</color>
    <!-- Darkest shade of the accent color used by the android_system. Black.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent1_1000">#000000</color>

    <!-- Lightest shade of the secondary accent color used by the android_system. White.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_0">#ffffff</color>
    <!-- Shade of the secondary accent android_system color at 99% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_10">#F0FFFC</color>
    <!-- Shade of the secondary accent android_system color at 95% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_50">#CDFAF1</color>
    <!-- Shade of the secondary accent android_system color at 90% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_100">#BFEBE3</color>
    <!-- Shade of the secondary accent android_system color at 80% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_200">#A4CFC7</color>
    <!-- Shade of the secondary accent android_system color at 70% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_300">#89B4AC</color>
    <!-- Shade of the secondary accent android_system color at 60% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_400">#6F9991</color>
    <!-- Shade of the secondary accent android_system color at 49% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_500">#537C75</color>
    <!-- Shade of the secondary accent android_system color at 40% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_600">#3D665F</color>
    <!-- Shade of the secondary accent android_system color at 30% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_700">#254E47</color>
    <!-- Shade of the secondary accent android_system color at 20% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_800">#0C3731</color>
    <!-- Shade of the secondary accent android_system color at 10% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_900">#00211C</color>
    <!-- Darkest shade of the secondary accent color used by the android_system. Black.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent2_1000">#000000</color>

    <!-- Lightest shade of the tertiary accent color used by the android_system. White.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_0">#ffffff</color>
    <!-- Shade of the tertiary accent android_system color at 99% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_10">#FFFBFF</color>
    <!-- Shade of the tertiary accent android_system color at 95% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_50">#F9EAFF</color>
    <!-- Shade of the tertiary accent android_system color at 90% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_100">#ECDBFF</color>
    <!-- Shade of the tertiary accent android_system color at 80% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_200">#CFBFEB</color>
    <!-- Shade of the tertiary accent android_system color at 70% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_300">#B3A4CF</color>
    <!-- Shade of the tertiary accent android_system color at 60% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_400">#988AB3</color>
    <!-- Shade of the tertiary accent android_system color at 49% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_500">#7B6E96</color>
    <!-- Shade of the tertiary accent android_system color at 40% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_600">#64587F</color>
    <!-- Shade of the tertiary accent android_system color at 30% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_700">#4C4165</color>
    <!-- Shade of the tertiary accent android_system color at 20% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_800">#352B4D</color>
    <!-- Shade of the tertiary accent android_system color at 10% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_900">#1E1636</color>
    <!-- Darkest shade of the tertiary accent color used by the android_system. Black.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_accent3_1000">#000000</color>

    <!-- Lightest shade of the neutral color used by the android_system. White.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_0">#ffffff</color>
    <!-- Shade of the neutral android_system color at 99% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_10">#fbfbfb</color>
    <!-- Shade of the neutral android_system color at 95% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_50">#f0f0f0</color>
    <!-- Shade of the neutral android_system color at 90% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_100">#e2e2e2</color>
    <!-- Shade of the neutral android_system color at 80% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_200">#c6c6c6</color>
    <!-- Shade of the neutral android_system color at 70% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_300">#ababab</color>
    <!-- Shade of the neutral android_system color at 60% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_400">#909090</color>
    <!-- Shade of the neutral android_system color at 49% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_500">#757575</color>
    <!-- Shade of the neutral android_system color at 40% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_600">#5e5e5e</color>
    <!-- Shade of the neutral android_system color at 30% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_700">#464646</color>
    <!-- Shade of the neutral android_system color at 20% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_800">#303030</color>
    <!-- Shade of the neutral android_system color at 10% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_900">#1b1b1b</color>
    <!-- Darkest shade of the neutral color used by the android_system. Black.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral1_1000">#000000</color>

    <!-- Lightest shade of the secondary neutral color used by the android_system. White.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_0">#ffffff</color>
    <!-- Shade of the secondary neutral android_system color at 99% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_10">#fbfbfb</color>
    <!-- Shade of the secondary neutral android_system color at 95% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_50">#f0f0f0</color>
    <!-- Shade of the secondary neutral android_system color at 90% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_100">#e2e2e2</color>
    <!-- Shade of the secondary neutral android_system color at 80% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_200">#c6c6c6</color>
    <!-- Shade of the secondary neutral android_system color at 70% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_300">#ababab</color>
    <!-- Shade of the secondary neutral android_system color at 60% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_400">#909090</color>
    <!-- Shade of the secondary neutral android_system color at 49% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_500">#757575</color>
    <!-- Shade of the secondary neutral android_system color at 40% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_600">#5e5e5e</color>
    <!-- Shade of the secondary neutral android_system color at 30% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_700">#464646</color>
    <!-- Shade of the secondary neutral android_system color at 20% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_800">#303030</color>
    <!-- Shade of the secondary neutral android_system color at 10% lightness.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_900">#1b1b1b</color>
    <!-- Darkest shade of the secondary neutral color used by the android_system. Black.
     This value can be overlaid at runtime by OverlayManager RROs. -->
    <color name="android_system_neutral2_1000">#000000</color>

</resources>